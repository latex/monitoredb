# MonitoreDB - Configuração do Servidor
MONITOREDB_SERVER_BIND=0.0.0.0
MONITOREDB_SERVER_PORT=3000

# Token de autenticação (ingest + escrita na API). Vazio = API aberta.
MONITOREDB_TOKEN=

# --- Docker Compose ---
MSSQL_SA_PASSWORD=troque-esta-senha

# --- Agente (config via args CLI ou env) ---
# MONITOREDB_AGENT_SERVER=http://localhost:3000
# MONITOREDB_AGENT_ID=
# MONITOREDB_AGENT_INTERVAL=30
# MONITOREDB_AGENT_TOKEN=
# MONITOREDB_SQL_HOST=localhost
# MONITOREDB_SQL_PORT=1433
# MONITOREDB_SQL_USERNAME=sa
# MONITOREDB_SQL_PASSWORD=senha
